# features/workspace

Tudo da Área de Trabalho do OS¹.

Subpastas previstas (criadas conforme as fases avançam):
- `tools/`       — catálogo, seletor, runner e UI das Ferramentas
- `blocks/`      — renderização dos blocos (initial, mode, standard, share, tool, diagnostico)
- `generation/`  — geração local de conteúdo (Entender/Executar/Aprender/Compartilhar) e regras sensíveis
- `history/`     — sessões arquivadas por setor

Regra: comportamento atual do ChatPanel é o contrato. Refatorar sem alterar
visual, texto, geração ou histórico.
